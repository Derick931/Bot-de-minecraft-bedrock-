<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Dashboard Bot Minecraft</title>
  <style>
    body { font-family: Arial, sans-serif; background: #222; color: #fff; text-align: center;}
    .estado { font-size: 2em; margin: 20px; }
    .info { margin: 20px; }
    button { font-size: 1.2em; padding: 10px 20px; }
  </style>
</head>
<body>
  <h1>Estado del Bot de Minecraft Bedrock</h1>
  <div class="estado" id="estado"></div>
  <div class="info" id="info"></div>
  <button id="activar">Activar Bot</button>

  <script>
    function actualizarEstado() {
      fetch('/estado')
        .then(res => res.json())
        .then(data => {
          document.getElementById('estado').innerText = data.activo ? 'Activo 🟢' : 'Inactivo 🔴';
          document.getElementById('info').innerText = data.info;
        });
    }

    document.getElementById('activar').onclick = function() {
      fetch('/activar', {method: 'POST'})
        .then(() => actualizarEstado());
    };

    actualizarEstado();
    setInterval(actualizarEstado, 5000); // actualiza cada 5 segundos
  </script>
</body>
</html>
